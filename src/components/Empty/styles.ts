import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 60,
    alignItems: "center",
  },
  text1: {
    fontWeight: "bold"
  },
  text2: {
    color: "#808080"
  }
});
